存储函数
	存储函数是有返回值的存储过程，存储函数的阐述只能是in类型，具体语法如下：
	create  function  存储函数名称([  param  ])
	
	returns  type  [characteristic ... ]
	begin
		---sql语句
		return  ...;
	end;
	
	characteristic说明
	·  deterministic：相同的输入参数总是产生相同的结果  --important
	·  no  sql：不包含SQL语句；
	·  READS  SQL  DATA：包含读取数据的语句，但不包含写入数据的语句；
